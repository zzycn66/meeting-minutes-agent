# 会议纪要智能体 — 纯代码讲解文档

---

# 一、main.py — 应用入口完整代码讲解

```python
# -*- coding: utf-8 -*-
"""会议纪要智能体 - 主应用入口"""
```
**解释**：第一行声明文件编码为 UTF-8，这是 Python 3 的标准做法，确保中文注释和字符串能正确处理。

```python
import os
import sys
import logging
```
**解释**：
- `os`：操作系统接口，用于文件路径操作
- `sys`：系统相关，用于修改 Python 路径
- `logging`：日志记录模块

```python
_backend_dir = os.path.dirname(os.path.abspath(__file__))
```
**解释**：
- `__file__`：当前 Python 文件的路径
- `os.path.abspath(__file__)`：获取绝对路径（如 `C:\xxx\main.py`）
- `os.path.dirname(...)`：获取目录部分（如 `C:\xxx\`）
- 最终 `_backend_dir` = 项目根目录的绝对路径

```python
if _backend_dir not in sys.path:
    sys.path.insert(0, _backend_dir)
```
**解释**：
- `sys.path` 是 Python 查找模块的路径列表
- 将项目根目录添加到最前面，确保能正确导入 `app` 包
- 这样做是为了支持从任意位置运行脚本（如 `python backend/main.py`）

```python
# 提前加载 .env 并设置 HF_ENDPOINT
_dotenv_path = os.path.join(_backend_dir, ".env")
if os.path.exists(_dotenv_path):
    try:
        from dotenv import load_dotenv
        load_dotenv(_dotenv_path)
    except ImportError:
        pass
```
**解释**：
- `.env` 文件存储敏感配置（如 API Key），不上传到 Git
- `load_dotenv` 将 `.env` 文件中的键值对加载到环境变量
- `try-except`：如果没安装 `python-dotenv` 库，跳过（优雅降级）
- **关键**：必须在导入 transformers 之前加载，因为 HuggingFace 在导入时就缓存了环境变量

```python
if "HF_ENDPOINT" not in os.environ:
    os.environ["HF_ENDPOINT"] = "https://hf-mirror.com"
```
**解释**：
- 设置 HuggingFace 镜像地址
- 国内直接访问 huggingface.co 很慢或无法访问
- `hf-mirror.com` 是国内镜像站，下载模型速度更快

```python
try:
    from transformers import AutoTokenizer, AutoModelForCausalLM
    import torch
    logging.getLogger(__name__).info("transformers/torch 预加载成功")
except ImportError as e:
    logging.getLogger(__name__).warning(f"transformers/torch 预加载失败: {e}")
```
**解释**：
- 提前导入深度学习框架，避免首次请求时的延迟
- `AutoTokenizer`：自动加载分词器
- `AutoModelForCausalLM`：自动加载因果语言模型（如 Qwen3）
- `torch`：PyTorch 深度学习框架
- 如果导入失败，只记录警告，不阻塞服务启动

```python
from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles
from fastapi.middleware.cors import CORSMiddleware
from fastapi.responses import FileResponse
```
**解释**：
- `FastAPI`：高性能异步 Web 框架
- `StaticFiles`：静态文件服务（前端 CSS/JS）
- `CORSMiddleware`：跨域资源共享中间件
- `FileResponse`：返回文件的 HTTP 响应

```python
from app.models import Meeting, MeetingMinutes, ActionItem, User, MeetingTemplate
from app.database import engine, Base
from app.routes import meetings, minutes, export, auth, templates, websocket, audio, public
from app.routes.qa import qa_router
from config import ALLOWED_ORIGINS
```
**解释**：
- 导入数据库模型（定义表结构）
- 导入数据库引擎和基类
- 导入所有 API 路由模块
- 导入 CORS 配置

```python
logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s [%(levelname)s] %(name)s: %(message)s",
    datefmt="%Y-%m-%d %H:%M:%S",
)
```
**解释**：
- 配置日志格式
- `%(asctime)s`：时间戳
- `%(levelname)s`：日志级别（INFO/WARNING/ERROR）
- `%(name)s`：模块名
- `%(message)s`：日志消息
- 输出示例：`2024-01-15 10:30:00 [INFO] main: 服务启动成功`

```python
app = FastAPI(
    title="会议纪要智能体",
    description="智能会议纪要助手 - 自动转写、提取、生成结构化会议纪要",
    version="1.0.0",
)
```
**解释**：
- 创建 FastAPI 应用实例
- 这些元数据会显示在 API 文档页面（访问 `/docs` 可以看到）
- 自动生成 Swagger/OpenAPI 文档

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=ALLOWED_ORIGINS,
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```
**解释**：
- **CORS（跨域资源共享）**：浏览器安全策略，前端默认不能访问不同域名的后端 API
- `allow_origins`：允许哪些域名访问（从 config.py 读取，默认 `http://localhost:8000`）
- `allow_credentials`：允许携带 Cookie（用户认证需要）
- `allow_methods`：允许所有 HTTP 方法（GET/POST/PUT/DELETE）
- `allow_headers`：允许所有请求头

```python
app.include_router(meetings.router)
app.include_router(minutes.router)
app.include_router(export.router)
app.include_router(auth.router)
app.include_router(templates.router)
app.include_router(websocket.router)
app.include_router(audio.audio_router)
app.include_router(public.router)
app.include_router(qa_router)
```
**解释**：
- `include_router`：将路由模块注册到主应用
- 每个路由模块定义了一组相关的 API 端点
- 例如 `meetings.router` 处理 `/api/meetings/*` 的请求

```python
if os.path.exists(FRONTEND_DIR):
    app.mount("/static", StaticFiles(directory=FRONTEND_DIR), name="static")
```
**解释**：
- `mount`：挂载静态文件服务
- 访问 `/static/css/style.css` 会返回 `frontend/css/style.css` 文件
- 前端的 HTML/CSS/JS 文件通过这个方式提供服务

```python
@app.on_event("startup")
async def startup():
    Base.metadata.create_all(bind=engine)
```
**解释**：
- `@app.on_event("startup")`：FastAPI 生命周期钩子，服务启动时自动执行
- `Base.metadata.create_all(bind=engine)`：根据模型定义创建数据库表（如果不存在）
- 这相当于执行 SQL 的 `CREATE TABLE IF NOT EXISTS`

```python
    import threading
    def _preload():
        logger.info("Pre-loading faster-whisper model...")
        from app.services.speech_service import _get_faster_whisper_model
        m = _get_faster_whisper_model()
        logger.info("Model pre-loaded: %s", "OK" if m else "FAILED")
    threading.Thread(target=_preload, daemon=True).start()
```
**解释**：
- Whisper 模型加载需要几秒到几十秒
- 使用 `threading.Thread` 在后台线程加载，不阻塞服务启动
- `daemon=True`：主线程退出时自动终止子线程
- 这样服务启动后就能立即接受请求，模型在后台慢慢加载

```python
@app.get("/")
async def index():
    index_path = os.path.join(FRONTEND_DIR, "index.html")
    if os.path.exists(index_path):
        return FileResponse(index_path, headers={
            "Cache-Control": "no-cache, no-store, must-revalidate",
            "Pragma": "no-cache",
            "Expires": "0"
        })
    return {"message": "会议纪要智能体API服务运行中", "docs": "/docs"}
```
**解释**：
- `@app.get("/")`：定义 GET 请求的根路径路由
- 返回前端首页 `index.html`
- **缓存控制头**：
  - `Cache-Control: no-cache`：每次请求都要验证
  - `Pragma: no-cache`：HTTP/1.0 兼容
  - `Expires: 0`：立即过期
  - 目的：确保用户总是获取最新版本的前端代码

```python
@app.get("/health")
async def health():
    return {"status": "ok", "service": "Meeting Minutes Agent"}
```
**解释**：
- 健康检查接口，用于监控服务状态
- 可用于负载均衡器或监控系统

```python
if __name__ == "__main__":
    import uvicorn
    from config import HOST, PORT
    Base.metadata.create_all(bind=engine)
    uvicorn.run("main:app", host=HOST, port=PORT, reload=False)
```
**解释**：
- `if __name__ == "__main__"`：直接运行脚本时执行
- `uvicorn.run`：启动 Uvicorn ASGI 服务器
- `"main:app"`：应用入口（模块名:应用变量名）
- `reload=False`：生产环境关闭热重载

---

# 二、config.py — 配置系统完整代码讲解

```python
import os
from dotenv import load_dotenv

load_dotenv(os.path.join(os.path.dirname(os.path.abspath(__file__)), ".env"))
```
**解释**：
- 加载 `.env` 文件中的环境变量
- `.env` 文件示例：
  ```
  DEEPSEEK_API_KEY=sk-xxxx
  MIMO_API_KEY=xxxx
  WHISPER_MODEL_SIZE=medium
  ```

```python
BASE_DIR = os.path.dirname(os.path.abspath(__file__))
```
**解释**：
- `BASE_DIR`：项目根目录的绝对路径
- 后续所有路径都基于此构建

```python
DATABASE_URL = f"sqlite:///{os.path.join(BASE_DIR, 'meeting_agent.db')}"
```
**解释**：
- SQLite 数据库连接字符串
- 格式：`sqlite:///数据库文件路径`
- 数据库文件：`meeting_agent.db`（单文件数据库）
- SQLite 优势：轻量级、免安装、适合中小型应用

```python
MIMO_API_KEY = os.environ.get("MIMO_API_KEY", "")
MIMO_ASR_BASE_URL = os.environ.get("MIMO_ASR_BASE_URL", "https://api.xiaomimimo.com/v1")
```
**解释**：
- MiMo ASR 是小米的云端语音识别服务
- `os.environ.get("key", "default")`：优先读取环境变量，不存在则使用默认值
- 这样 API Key 不会硬编码在代码中，更安全

```python
DEEPSEEK_API_KEY = os.environ.get("DEEPSEEK_API_KEY", "")
DEEPSEEK_BASE_URL = os.environ.get("DEEPSEEK_BASE_URL", "https://api.deepseek.com")
DEEPSEEK_MODEL = os.environ.get("DEEPSEEK_MODEL", "deepseek-chat")
```
**解释**：
- DeepSeek 是大语言模型 API 服务
- `deepseek-chat`：对话模型，支持中文
- API 兼容 OpenAI 格式，可以直接用 OpenAI SDK 调用

```python
LOCAL_MODEL_NAME = os.environ.get("LOCAL_MODEL_NAME", "Qwen/Qwen3-0.6B")
LOCAL_MODEL_CACHE = os.environ.get("LOCAL_MODEL_CACHE", "")
LOCAL_MODEL_DEVICE = os.environ.get("LOCAL_MODEL_DEVICE", "cpu")
LOCAL_MODEL_MAX_TOKENS = int(os.environ.get("LOCAL_MODEL_MAX_TOKENS", "1024"))
```
**解释**：
- `Qwen/Qwen3-0.6B`：通义千问 3 代 0.6B 参数模型
- 模型大小选择：

| 模型 | 参数量 | 内存需求 | 速度 | 准确率 |
|------|--------|----------|------|--------|
| 0.6B | 6亿 | ~2GB | 快 | 一般 |
| 1.7B | 17亿 | ~4GB | 中等 | 较好 |
| 4B | 40亿 | ~8GB | 较慢 | 好 |
| 8B | 80亿 | ~16GB | 慢 | 很好 |

- `LOCAL_MODEL_DEVICE`：运行设备
  - `cpu`：CPU 推理（较慢，但无需 GPU）
  - `cuda`：GPU 推理（需要 NVIDIA GPU）
- `LOCAL_MODEL_MAX_TOKENS`：最大输出 token 数

```python
WHISPER_MODEL_SIZE = os.environ.get("WHISPER_MODEL_SIZE", "medium")
```
**解释**：
- Whisper 模型大小选择：

| 模型 | 参数量 | 内存需求 | 速度 | 准确率 |
|------|--------|----------|------|--------|
| tiny | 39M | ~1GB | 最快 | 最低 |
| base | 74M | ~1GB | 快 | 低 |
| small | 244M | ~2GB | 中等 | 中等 |
| medium | 769M | ~5GB | 较慢 | 高 |
| large-v3 | 1550M | ~10GB | 最慢 | 最高 |

- 推荐：`medium`（平衡速度和准确率）

```python
HF_ENDPOINT = os.environ.get("HF_ENDPOINT", "https://hf-mirror.com")
```
**解释**：
- HuggingFace 镜像地址
- 国内访问 huggingface.co 很慢
- 使用镜像站加速模型下载

```python
UPLOAD_DIR = os.path.join(os.path.dirname(BASE_DIR), "frontend", "uploads")
MAX_UPLOAD_SIZE = 100 * 1024 * 1024  # 100MB
ALLOWED_AUDIO_EXTENSIONS = {"mp3", "wav", "m4a", "aac", "wma", "ogg", "flac", "webm"}
```
**解释**：
- `UPLOAD_DIR`：上传文件存储目录
- `MAX_UPLOAD_SIZE`：最大上传大小 100MB
- `ALLOWED_AUDIO_EXTENSIONS`：允许的音频格式集合（set 数据结构）

```python
HOST = "0.0.0.0"
PORT = 8000
```
**解释**：
- `HOST = "0.0.0.0"`：监听所有网络接口（包括 localhost 和局域网 IP）
- `PORT = 8000`：监听端口

---

# 三、database.py — 数据库层完整代码讲解

```python
import time
import logging
from sqlalchemy import create_engine, event
from sqlalchemy.orm import sessionmaker, declarative_base
from config import DATABASE_URL
```
**解释**：
- `create_engine`：创建数据库引擎
- `event`：SQLAlchemy 事件监听器
- `sessionmaker`：创建会话类
- `declarative_base`：声明式基类（所有模型继承此类）

```python
engine = create_engine(
    DATABASE_URL,
    connect_args={"check_same_thread": False},
    pool_pre_ping=True,
    pool_size=5,
    max_overflow=5,
    pool_recycle=3600,
    echo=False,
)
```
**解释**：
- `connect_args={"check_same_thread": False}`：SQLite 默认不允许跨线程访问，此参数禁用此检查
- `pool_pre_ping=True`：每次使用连接前执行 `SELECT 1` 检测连接是否有效
- `pool_size=5`：连接池保持 5 个空闲连接
- `max_overflow=5`：超出 pool_size 的最大连接数（最多 10 个并发连接）
- `pool_recycle=3600`：连接使用超过 1 小时后回收
- `echo=False`：不打印 SQL 语句（生产环境）

```python
@event.listens_for(engine, "connect")
def _set_sqlite_pragma(dbapi_connection, connection_record):
    cursor = dbapi_connection.cursor()
    cursor.execute("PRAGMA journal_mode=WAL")
    cursor.execute("PRAGMA synchronous=NORMAL")
    cursor.execute("PRAGMA cache_size=-64000")
    cursor.execute("PRAGMA temp_store=MEMORY")
    cursor.close()
```
**解释**：
- PRAGMA 是 SQLite 的配置命令
- `journal_mode=WAL`：启用 WAL 模式，允许读写并发
  - 普通模式：读写互斥，写操作阻塞读操作
  - WAL 模式：读写可以并发，性能提升 2-5 倍
- `synchronous=NORMAL`：平衡性能和安全
- `cache_size=-64000`：64MB 缓存（负数表示 KB）
- `temp_store=MEMORY`：临时表存储在内存（更快）

```python
@event.listens_for(engine, "before_cursor_execute")
def _log_slow_queries(conn, cursor, statement, parameters, context, executemany):
    conn.info.setdefault("query_start_time", []).append(time.monotonic())

@event.listens_for(engine, "after_cursor_execute")
def _log_query_duration(conn, cursor, statement, parameters, context, executemany):
    start_times = conn.info.get("query_start_time", [])
    if start_times:
        start_time = start_times.pop()
        duration = time.monotonic() - start_time
        if duration > 1.0:
            logger.warning("Slow query (%.2fs): %s", duration, statement[:200])
```
**解释**：
- 使用 SQLAlchemy 事件监听器记录查询耗时
- `before_cursor_execute`：SQL 执行前记录时间
- `after_cursor_execute`：SQL 执行后计算耗时
- 超过 1 秒的查询记录为慢查询

```python
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()
```
**解释**：
- `sessionmaker`：创建会话类
  - `autocommit=False`：不自动提交，需要手动 `db.commit()`
  - `autoflush=False`：不自动刷新
- `declarative_base()`：声明式基类，所有模型继承此类

```python
def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```
**解释**：
- FastAPI 依赖注入函数
- `yield`：返回数据库会话，同时保持函数运行
- `finally`：确保会话关闭（无论成功或失败）
- 使用方式：`db: Session = Depends(get_db)`

---

# 四、models.py — 数据库模型完整代码讲解

```python
class Meeting(Base):
    """会议记录表"""
    __tablename__ = "meetings"

    id = Column(Integer, primary_key=True, autoincrement=True)
    title = Column(String(200), nullable=False, comment="会议标题")
    meeting_date = Column(DateTime, default=datetime.now, comment="会议日期")
    duration = Column(String(50), comment="会议时长")
    participants = Column(String(500), comment="参会人员")
    input_type = Column(String(20), default="text", comment="输入类型: audio/text")
    raw_text = Column(Text, comment="原始文本/转写文本")
    audio_path = Column(String(500), comment="音频文件路径")
    status = Column(String(20), default="processing", comment="状态")
    is_public = Column(Boolean, default=False, comment="是否公开")
    created_by = Column(Integer, ForeignKey("users.id"), comment="创建者ID")
    created_at = Column(DateTime, default=datetime.now, comment="创建时间")
    updated_at = Column(DateTime, default=datetime.now, onupdate=datetime.now, comment="更新时间")
```
**解释**：
- `__tablename__ = "meetings"`：对应数据库中的表名
- `primary_key=True`：主键，唯一标识每条记录
- `autoincrement=True`：自增，新记录自动分配 ID
- `nullable=False`：不允许为空
- `default=datetime.now`：默认值为当前时间（注意不要加括号）
- `onupdate=datetime.now`：更新时自动更新时间戳
- `ForeignKey("users.id")`：外键关联 users 表

```python
    creator = relationship("User")
    minutes = relationship("MeetingMinutes", back_populates="meeting", uselist=False, cascade="all, delete-orphan")
    action_items = relationship("ActionItem", back_populates="meeting", cascade="all, delete-orphan")
```
**解释**：
- `relationship("User")`：多对一关系，一个会议属于一个用户
- `relationship("MeetingMinutes", uselist=False)`：一对一关系，返回单个对象
- `relationship("ActionItem")`：一对多关系，返回列表
- `cascade="all, delete-orphan"`：级联删除，删除会议时自动删除关联的纪要和行动项

```python
class MeetingMinutes(Base):
    """会议纪要表"""
    __tablename__ = "meeting_minutes"

    id = Column(Integer, primary_key=True, autoincrement=True)
    meeting_id = Column(Integer, ForeignKey("meetings.id"), unique=True, comment="关联会议ID")
    topic = Column(String(200), comment="会议议题")
    overview = Column(Text, comment="会议概述")
    discussion_points = Column(Text, comment="讨论要点")
    key_decisions = Column(Text, comment="关键决策")
    unresolved_issues = Column(Text, comment="遗留问题")
    summary = Column(Text, comment="会议总结")
    markdown = Column(Text, comment="生成的Markdown")
    html = Column(Text, comment="生成的HTML")
    created_at = Column(DateTime, default=datetime.now)

    meeting = relationship("Meeting", back_populates="minutes")
```
**解释**：
- `unique=True`：唯一约束，确保每个会议只有一份纪要
- `back_populates="minutes"`：双向关系，从会议可以访问纪要

```python
class ActionItem(Base):
    """行动项表"""
    __tablename__ = "action_items"
    __table_args__ = (
        Index("ix_action_items_meeting_id", "meeting_id"),
    )

    id = Column(Integer, primary_key=True, autoincrement=True)
    meeting_id = Column(Integer, ForeignKey("meetings.id"), comment="关联会议ID")
    content = Column(Text, nullable=False, comment="行动项内容")
    responsible_person = Column(String(100), comment="责任人")
    deadline = Column(String(50), comment="截止时间")
    priority = Column(String(20), default="medium", comment="优先级")
    status = Column(String(20), default="pending", comment="状态")
    created_at = Column(DateTime, default=datetime.now)

    meeting = relationship("Meeting", back_populates="action_items")
```
**解释**：
- `Index("ix_action_items_meeting_id", "meeting_id")`：创建索引，加速按 meeting_id 查询
- `priority`：high/medium/low
- `status`：pending/in_progress/completed

---

# 五、speech_service.py — 语音识别模块完整代码讲解

```python
import os
import re
import logging

from config import WHISPER_MODEL_SIZE, MIMO_API_KEY, MIMO_ASR_BASE_URL
```
**解释**：
- 导入配置项
- `WHISPER_MODEL_SIZE`：Whisper 模型大小
- `MIMO_API_KEY`：小米 ASR API 密钥

```python
_faster_whisper_model = None
_whisper_model = None
```
**解释**：
- 全局变量，用于缓存模型（单例模式）
- 避免每次请求都重新加载模型

```python
def _get_faster_whisper_model():
    global _faster_whisper_model
    if _faster_whisper_model is not None:
        return _faster_whisper_model

    try:
        from faster_whisper import WhisperModel
        compute_type = "int8" if os.environ.get("LOCAL_MODEL_DEVICE", "cpu") == "cpu" else "float16"
        logger.info("Loading faster-whisper model: %s, compute_type=%s", WHISPER_MODEL_SIZE, compute_type)
        _faster_whisper_model = WhisperModel(
            WHISPER_MODEL_SIZE,
            device=os.environ.get("LOCAL_MODEL_DEVICE", "cpu"),
            compute_type=compute_type,
        )
        logger.info("faster-whisper model loaded")
        return _faster_whisper_model
    except ImportError:
        logger.warning("faster-whisper not installed, falling back to openai-whisper")
        return None
```
**解释**：
- `global _faster_whisper_model`：声明使用全局变量
- 检查缓存，如果已加载则直接返回
- `faster_whisper`：基于 CTranslate2 加速的 Whisper，速度提升 2-4 倍
- `compute_type="int8"`：CPU 使用 int8 量化，更快
- `compute_type="float16"`：GPU 使用 float16，更准
- 如果 `faster_whisper` 未安装，返回 None，降级到原版 Whisper

```python
class SpeechRecognizer:
    def recognize_file(self, filepath: str, asr_engine: str = "") -> dict:
        logger.info(f"Speech recognition started | engine={asr_engine or 'auto'} | file={filepath}")
        
        enhanced_path = self._enhance_if_needed(filepath)
        
        result = None
        
        if asr_engine == "whisper" or not asr_engine:
            try:
                result = self._recognize_with_whisper(enhanced_path)
                result["engine"] = "whisper"
            except Exception as e:
                logger.warning(f"Whisper recognition failed: {e}")
                if asr_engine == "whisper":
                    return {"success": False, "message": f"Whisper 识别失败: {str(e)}"}
                logger.info("Falling back to MiMo ASR")
        
        if result is None and (asr_engine == "mimo" or not asr_engine):
            try:
                result = self._recognize_with_mimo(enhanced_path)
                result["engine"] = "mimo"
            except Exception as e:
                logger.error(f"MiMo recognition failed: {e}")
                return {"success": False, "message": f"识别失败: {str(e)}"}
        
        if result and result.get("text"):
            cleaned_text = self._post_process(result["text"])
            result["text"] = cleaned_text
        
        return result
```
**解释**：
- 双引擎架构：优先使用 Whisper（免费），失败降级到 MiMo
- `asr_engine == "whisper"`：用户指定使用 Whisper
- `asr_engine == "mimo"`：用户指定使用 MiMo
- `not asr_engine`：自动选择（优先 Whisper）
- `_post_process`：后处理（繁简转换、幻觉过滤等）

```python
def _recognize_with_whisper(self, filepath: str) -> dict:
    model = _get_faster_whisper_model()
    
    if model:
        segments, info = model.transcribe(
            filepath,
            language="zh",
            beam_size=5,
            best_of=5,
            temperature=0.0,
            condition_on_previous_text=False,
            initial_prompt=WHISPER_INITIAL_PROMPT,
        )
        
        text = "".join([seg.text for seg in segments])
        return {"success": True, "text": text}
    else:
        model = _get_whisper_model()
        result = model.transcribe(filepath, **WHISPER_TRANSCRIBE_KWARGS)
        return {"success": True, "text": result["text"]}
```
**解释**：
- `language="zh"`：指定中文，提升识别准确率
- `beam_size=5`：束搜索宽度，越大越准但越慢
- `best_of=5`：生成 5 个候选，选择最佳
- `temperature=0.0`：贪心解码，确定性输出
- `condition_on_previous_text=False`：不依赖前文，防止幻觉累积
- `initial_prompt`：初始提示词，提供上下文
- `segments`：识别结果的片段列表
- `"".join([seg.text for seg in segments])`：拼接所有片段的文本

```python
REPEAT_CHAR = re.compile(r'(.)\1{4,}')
REPEAT_WORD = re.compile(r'(\S{2,6})\1{3,}')
REPEAT_PHRASE = re.compile(r'(.{2,10})\1{2,}')

WHISPER_INITIAL_PROMPT = "以下是一段中文会议录音的文字记录。"

def detect_prompt_echo(text: str) -> bool:
    if not text:
        return True
    if text.strip().rstrip("。") == WHISPER_INITIAL_PROMPT.rstrip("。"):
        return True
    if len(text) < 40 and any(frag in text for frag in WHISPER_PROMPT_FRAGMENTS):
        return True
    return False

def detect_repetition(text: str) -> bool:
    if not text or len(text) < 4:
        return False
    if REPEAT_CHAR.search(text):
        return True
    if REPEAT_WORD.search(text):
        return True
    if REPEAT_PHRASE.search(text):
        return True
    return False
```
**解释**：
- `REPEAT_CHAR`：检测单字符重复 5 次以上（如 "哈哈哈哈哈哈哈"）
- `REPEAT_WORD`：检测词语重复 3 次以上（如 "那个那个那个那个"）
- `REPEAT_PHRASE`：检测短语重复 2 次以上（如 "对吧对吧对吧"）
- `detect_prompt_echo`：检测 Whisper 是否只输出了初始提示词（音频为空时）
- 这些是 Whisper 常见的幻觉问题

```python
_T2S_DICT = {
    "會": "会", "來": "来", "對": "对", "開": "开", "關": "关",
    "動": "动", "學": "学", "實": "实", "現": "现", "發": "发",
    "說": "说", "話": "话", "為": "为", "經": "经", "過": "过",
    # ... 约 200 个常用繁体字
}

def _traditional_to_simplified(self, text: str) -> str:
    try:
        from zhconv import convert
        return convert(text, "zh-cn")
    except ImportError:
        pass
    return ''.join(_T2S_DICT.get(char, char) for char in text)
```
**解释**：
- Whisper 可能输出繁体中文（特别是台湾地区口音）
- 优先使用 `zhconv` 库转换（最准确）
- 兜底使用字典映射（覆盖常用字）

---

# 六、nlp_service.py — NLP信息提取模块完整代码讲解

```python
DECISION_KEYWORDS = [
    "决定", "确定", "同意", "通过", "批准", "拍板", "敲定",
    "达成共识", "一致认为", "最终方案", "砍掉", "放弃",
    "推迟", "延后", "搁置", "优先做", "先做"
]

ACTION_KEYWORDS = [
    "负责", "由.*完成", "安排.*做", "指派", "落实", "执行",
    "推进", "下一步", "上线", "启动", "搞定", "出报告"
]

ISSUE_KEYWORDS = [
    "问题", "未解决", "待讨论", "需要进一步", "遗留", "暂缓",
    "延后", "搁置", "尚需", "还需", "继续讨论", "下次会议"
]
```
**解释**：
- 关键词库，用于匹配包含特定信息的句子
- `DECISION_KEYWORDS`：匹配关键决策
- `ACTION_KEYWORDS`：匹配行动项
- `ISSUE_KEYWORDS`：匹配遗留问题

```python
PERSON_PATTERNS = [
    r'(?:由|请|让|安排|指派|交给)([\u4e00-\u9fa5]{2,4}?)(?:负责|完成|跟进)',
    r'([\u4e00-\u9fa5]{2,4})(?:负责|来负责|来完成)',
    r'@([\u4e00-\u9fa5]{2,4})',
]
```
**解释**：
- 正则表达式，用于提取人名
- `(?:由|请|让...)`：非捕获组，匹配但不保存
- `([\u4e00-\u9fa5]{2,4}?)`：捕获组，保存 2-4 个中文字符
- `(?:负责|完成|跟进)`：非捕获组
- 示例：`"由张三负责"` → 提取出 `"张三"`

```python
DEADLINE_PATTERNS = [
    r'(截止|deadline)[：:]\s*(.+?)(?:[。；\n]|$)',
    r'(\d+月\d+[日号])',
    r'(\d{4}[-/]\d{1,2}[-/]\d{1,2})',
    r'(下周[一二三四五六日])',
    r'(今天|明天|后天)',
]
```
**解释**：
- 正则表达式，用于提取截止时间
- 示例：
  - `"截止：下周五"` → 提取出 `"下周五"`
  - `"1月15日"` → 提取出 `"1月15日"`
  - `"2024-01-15"` → 提取出 `"2024-01-15"`

```python
class NLPProcessor:
    def extract_action_items(self, text: str, _corrected: bool = False) -> list:
        if not _corrected:
            text = self._correct_homophone(text)
        sentences = self.split_sentences(text, _corrected=True)
        items = []
        seen = set()
        _PRONOUN_CHARS = set("我你他她它")
        _SKIP_PERSON_PREFIXES = {"我", "你", "他", "她", "它", "我们", "你们", "他们"}

        for sent in sentences:
            if not self._sentence_has_keywords(sent, _ACTION_KW_RE) and not _ACTION_PATTERNS_RE.search(sent):
                continue

            person = ""
            for pattern in _RE_PERSON_PATTERNS:
                match = pattern.search(sent)
                if match:
                    for g in match.groups():
                        if g and _RE_PERSON_CHECK.match(g) and not any(ch in _PRONOUN_CHARS for ch in g):
                            if g not in _SKIP_PERSON_PREFIXES:
                                person = g
                                break
                    if person:
                        break

            if not person:
                pos_persons = self._extract_persons_by_pos(sent)
                if pos_persons:
                    for p in pos_persons:
                        if p not in _SKIP_PERSON_PREFIXES:
                            person = p
                            break

            deadline = ""
            for pattern in _RE_DEADLINE_PATTERNS:
                match = pattern.search(sent)
                if match:
                    deadline = match.group(0)
                    deadline = re.sub(r'^(截止|截止时间|截止日期|deadline)[：:]\s*', '', deadline)
                    break

            cleaned = re.sub(r'^[，,。.！!？?\s]+', '', sent)
            cleaned = re.sub(r'[，,。.！!？?\s]+$', '', cleaned)
            if not cleaned or len(cleaned) < 8:
                continue

            skip_content_words = {"好", "行", "对", "嗯", "啊", "可以", "没问题", "好的", "明白"}
            if cleaned in skip_content_words:
                continue

            key = (person, cleaned[:30])
            if key in seen:
                continue
            seen.add(key)
            items.append({
                "content": cleaned,
                "responsible_person": person,
                "deadline": deadline,
                "priority": "medium"
            })
        return items
```
**解释**：
- 提取行动项的完整流程：
  1. 按句子分割文本
  2. 检查每个句子是否包含行动项关键词
  3. 提取责任人（先用正则，失败再用 jieba 词性标注）
  4. 提取截止时间
  5. 清理任务内容（去除标点、过滤过短内容）
  6. 去重（使用 `(person, content[:30])` 作为去重键）

```python
def extract_participants(self, text: str, _corrected: bool = False) -> str:
    if not _corrected:
        text = self._correct_homophone(text)
    persons = set()
    _PRONOUN_CHARS = set("我你他她它")
    
    # 策略 1：显式标签提取
    m = _RE_PARTICIPANT_LABEL.search(text)
    if m:
        for name in _RE_PARTICIPANT_SPLIT.split(m.group(1)):
            name = name.strip()
            if 2 <= len(name) <= 4 and _RE_PERSON_CHECK.match(name):
                if name not in _NOT_PERSON_WORDS:
                    persons.add(name)
    
    # 策略 2：说话人检测
    for name in self._detect_speakers(text):
        if name not in _NOT_PERSON_WORDS and not any(ch in _PRONOUN_CHARS for ch in name):
            persons.add(name)
    
    # 策略 3：词性标注提取
    if self.jieba_loaded:
        for word, flag in pseg.cut(text):
            if flag == "nr" and 2 <= len(word) <= 4:
                if word not in _SKIP_WORDS and word not in _NOT_PERSON_WORDS:
                    if not any(ch in _PRONOUN_CHARS for ch in word):
                        persons.add(word)
    
    return "、".join(persons) if persons else ""
```
**解释**：
- 三种策略提取参会人员：
  1. 显式标签：匹配 `"参会人：张三、李四"` 格式
  2. 说话人检测：匹配 `"张三说..."`、`"李四认为..."` 格式
  3. 词性标注：使用 jieba 识别词性为 `nr`（人名）的词
- 使用 `set` 自动去重
- 使用 `、` 连接返回

```python
class DeepSeekNLPProcessor:
    def _call_deepseek(self, user_message: str) -> Optional[dict]:
        try:
            from openai import OpenAI

            client = OpenAI(
                api_key=self.api_key,
                base_url=self.base_url,
            )

            response = client.chat.completions.create(
                model=self.model,
                messages=[
                    {"role": "system", "content": DEEPSEEK_SYSTEM_PROMPT},
                    {"role": "user", "content": user_message},
                ],
                temperature=0.3,
                max_tokens=4096,
            )

            content = response.choices[0].message.content.strip()

            if content.startswith("```"):
                lines = content.split("\n")
                if lines[0].startswith("```"):
                    lines = lines[1:]
                if lines and lines[-1].startswith("```"):
                    lines = lines[:-1]
                content = "\n".join(lines)

            return json.loads(content)

        except ImportError:
            logger.critical("openai 库未安装！请执行: pip install openai")
            return None
        except json.JSONDecodeError:
            logger.error(f"JSON 解析失败")
            return None
        except Exception as e:
            logger.error(f"API 调用异常: {e}")
            return None
```
**解释**：
- 使用 OpenAI SDK 调用 DeepSeek API
- `temperature=0.3`：低温度，输出更确定
- `max_tokens=4096`：最大输出 token 数
- 清理 markdown 代码块包裹（DeepSeek 可能返回 \`\`\`json ... \`\`\`）
- 解析 JSON 响应

```python
class LocalQwenProcessor:
    _model = None
    _tokenizer = None

    def _ensure_model(self):
        if LocalQwenProcessor._model is not None:
            return

        from transformers import AutoTokenizer, AutoModelForCausalLM
        import torch

        tokenizer = AutoTokenizer.from_pretrained(
            self.model_name,
            cache_dir=self.cache_dir,
            trust_remote_code=True,
        )

        if self.device == "cuda":
            model = AutoModelForCausalLM.from_pretrained(
                self.model_name,
                cache_dir=self.cache_dir,
                trust_remote_code=True,
                torch_dtype=torch.float16,
                device_map="auto",
                low_cpu_mem_usage=True,
            )
        else:
            model = AutoModelForCausalLM.from_pretrained(
                self.model_name,
                cache_dir=self.cache_dir,
                trust_remote_code=True,
                torch_dtype=torch.float32,
                low_cpu_mem_usage=True,
            )

        model.eval()
        LocalQwenProcessor._tokenizer = tokenizer
        LocalQwenProcessor._model = model
```
**解释**：
- 懒加载模型：首次调用时加载，后续复用
- `AutoTokenizer.from_pretrained`：自动加载分词器
- `AutoModelForCausalLM.from_pretrained`：自动加载模型
- `trust_remote_code=True`：允许加载自定义代码
- `device_map="auto"`：自动分配设备（CPU/GPU）
- `model.eval()`：设置为评估模式（关闭 dropout 等）

```python
    def _call_qwen(self, user_message: str, title: str = "", participants: str = "", 
                   max_tokens: int = 0, progress_callback=None) -> Optional[dict]:
        import torch
        from transformers import TextIteratorStreamer
        import threading

        tokenizer = LocalQwenProcessor._tokenizer
        model = LocalQwenProcessor._model

        messages = [
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_message},
        ]
        prompt = tokenizer.apply_chat_template(
            messages,
            tokenize=False,
            add_generation_prompt=True,
            enable_thinking=False,
        )

        inputs = tokenizer(prompt, return_tensors="pt", truncation=True, max_length=32768)

        streamer = TextIteratorStreamer(
            tokenizer,
            skip_prompt=True,
            skip_special_tokens=True
        )

        gen_kwargs = dict(
            **inputs,
            max_new_tokens=actual_max,
            do_sample=False,
            pad_token_id=tokenizer.eos_token_id,
            streamer=streamer,
        )

        def _generate():
            with torch.inference_mode():
                model.generate(**gen_kwargs)

        gen_thread = threading.Thread(target=_generate, daemon=True)
        gen_thread.start()

        generated_text = ""
        token_count = 0

        for new_text in streamer:
            generated_text += new_text
            token_count += 1
            if progress_callback and token_count % 2 == 0:
                progress = min(token_count / actual_max, 0.99)
                progress_callback(progress, token_count, actual_max, generated_text)

        gen_thread.join()

        content = generated_text.strip()

        if "<think>" in content and "</think>" in content:
            think_end = content.rfind("</think>")
            content = content[think_end + len("</think>"):].strip()

        try:
            result = json.loads(content)
            return result
        except json.JSONDecodeError:
            return None
```
**解释**：
- `tokenizer.apply_chat_template`：使用 Qwen3 的 chat template 格式化 prompt
- `enable_thinking=False`：禁用思维链，加速推理
- `TextIteratorStreamer`：流式输出器，逐 token 输出
- `torch.inference_mode()`：推理模式，节省内存
- `do_sample=False`：贪心解码，确定性输出
- 后台线程运行推理，主线程读取流式输出
- `<think>...</think>`：Qwen3 的思考过程，需要去除

---

# 七、qa_service.py — 智能问答模块完整代码讲解

```python
class QAService:
    _embedder = None

    def __init__(self):
        self._ensure_embedder()

    def _ensure_embedder(self):
        if QAService._embedder is not None:
            return
        if not SENTENCE_TRANSFORMERS_AVAILABLE:
            return
        try:
            QAService._embedder = SentenceTransformer(
                "paraphrase-multilingual-MiniLM-L12-v2"
            )
            logger.info("语义检索模型加载完成")
        except Exception as e:
            logger.warning("语义检索模型加载失败: %s", e)
```
**解释**：
- `SentenceTransformer`：句子嵌入模型
- `paraphrase-multilingual-MiniLM-L12-v2`：多语言支持，384 维向量
- 类变量 `_embedder`：全局共享，只加载一次

```python
    def _split_paragraphs(self, text: str, min_len: int = 10) -> list:
        paragraphs = []
        for p in text.split("\n"):
            p = p.strip()
            if len(p) >= min_len:
                paragraphs.append(p)
        if not paragraphs:
            for sent in text.replace("。", "。\n").replace("！", "！\n").replace("？", "？\n").split("\n"):
                sent = sent.strip()
                if len(sent) >= min_len:
                    paragraphs.append(sent)
        return paragraphs if paragraphs else [text] if text else []
```
**解释**：
- 先按换行符分割
- 如果段落太少，按句子分割（句号、感叹号、问号）
- 过滤过短的段落（少于 10 个字符）

```python
    def _semantic_search(self, paragraphs: list, question: str, top_k: int = 3) -> list:
        if not SENTENCE_TRANSFORMERS_AVAILABLE or QAService._embedder is None:
            return paragraphs[:top_k]
        try:
            para_embeddings = QAService._embedder.encode(paragraphs, convert_to_tensor=True)
            q_embedding = QAService._embedder.encode(question, convert_to_tensor=True)
            hits = util.semantic_search(q_embedding, para_embeddings, top_k=top_k)[0]
            return [paragraphs[hit["corpus_id"]] for hit in hits]
        except Exception as e:
            logger.warning("语义检索失败，降级返回前 %d 段: %s", top_k, e)
            return paragraphs[:top_k]
```
**解释**：
- `encode`：将文本编码为向量
- `semantic_search`：计算余弦相似度并排序
- `top_k=3`：返回最相关的 3 个段落
- `hit["corpus_id"]`：段落的索引
- 降级策略：语义检索失败时返回前 N 段

```python
    def _generate_answer(self, question: str, context: str) -> str:
        prompt = (
            f"你是会议纪要助手。根据以下会议文本回答问题。"
            f"即使信息不完全匹配，也请基于文本内容给出最相关的回答。\n\n"
            f"会议文本：\n{context}\n\n"
            f"问题：\n{question}\n\n"
            f"请直接回答："
        )
        
        try:
            from app.services.nlp_service import local_qwen_processor
            local_qwen_processor._ensure_model()
            
            tokenizer = local_qwen_processor.__class__._tokenizer
            model = local_qwen_processor.__class__._model
            
            messages = [{"role": "user", "content": prompt}]
            input_text = tokenizer.apply_chat_template(
                messages, tokenize=False, add_generation_prompt=True, enable_thinking=False
            )
            
            import torch
            inputs = tokenizer(input_text, return_tensors="pt", truncation=True, max_length=8192)
            
            with torch.inference_mode():
                output = model.generate(
                    **inputs, max_new_tokens=512, do_sample=False,
                    pad_token_id=tokenizer.eos_token_id
                )
            
            response = tokenizer.decode(output[0][inputs["input_ids"].shape[1]:], skip_special_tokens=True)
            return response.strip() if response.strip() else self._fallback_answer(question, context)
        except Exception as e:
            logger.warning("Qwen3 推理失败: %s", e)
            return self._fallback_answer(question, context)
```
**解释**：
- 构造 prompt：角色 + 上下文 + 问题
- 使用 Qwen3 本地模型推理
- `inputs["input_ids"].shape[1]`：输入 token 数，用于切分输出
- 降级策略：Qwen3 失败时使用关键词匹配

```python
    def _fallback_answer(self, question: str, context: str) -> str:
        keywords = [w for w in question if len(w) >= 2]
        matched = [line for line in context.split("\n") if any(k in line for k in keywords)]
        if matched:
            return "根据会议记录，相关段落如下：\n" + "\n".join(matched[:3])
        return "根据会议记录，与问题相关的信息如下：\n" + context[:500]
```
**解释**：
- 兜底方案：基于关键词匹配
- 提取问题中的关键词（长度 >= 2）
- 在上下文中搜索包含关键词的句子

```python
    def ask(self, meeting_id: int, question: str, db) -> dict:
        if not question or not question.strip():
            return {"success": False, "message": "问题不能为空"}

        meeting = db.query(Meeting).filter(Meeting.id == meeting_id).first()
        if not meeting:
            return {"success": False, "message": f"会议 ID {meeting_id} 不存在"}

        raw_text = meeting.raw_text or ""
        if not raw_text.strip():
            return {"success": False, "message": "该会议没有原始文本，无法回答问题"}

        paragraphs = self._split_paragraphs(raw_text)
        relevant = self._semantic_search(paragraphs, question)
        context = "\n".join(relevant)

        answer = self._generate_answer(question, context)

        return {
            "success": True,
            "answer": answer,
            "sources": relevant,
            "meeting_id": meeting_id,
            "question": question,
        }
```
**解释**：
- 问答主流程：
  1. 验证参数
  2. 获取会议原文
  3. 分割段落
  4. 语义检索相关段落
  5. 生成回答
  6. 返回结果（包含来源段落）

---

# 八、audio_processor.py — 音频增强模块完整代码讲解

```python
class AudioProcessor:
    def _check_dependencies(self):
        self.has_noisereduce = False
        self.has_pydub = False
        self.has_scipy = False

        try:
            import noisereduce
            self.has_noisereduce = True
        except ImportError:
            logger.warning("noisereduce 未安装，降噪功能不可用")

        try:
            from pydub import AudioSegment
            self.has_pydub = True
        except ImportError:
            logger.warning("pydub 未安装，音量标准化不可用")

        try:
            import scipy.signal
            self.has_scipy = True
        except ImportError:
            logger.warning("scipy 未安装，回声消除不可用")
```
**解释**：
- 检查依赖库是否可用
- 优雅降级：缺少某个库时禁用对应功能

```python
    def enhance_audio(self, input_path: str, output_path: str = None) -> dict:
        if not os.path.exists(input_path):
            return {"success": False, "message": f"音频文件不存在: {input_path}"}

        if output_path is None:
            output_path = input_path

        try:
            audio_segment = self._load_audio(input_path)
            if audio_segment is None:
                return {"success": False, "message": "无法加载音频文件"}

            if self.has_noisereduce:
                audio_segment = self._denoise(audio_segment)

            if self.has_pydub:
                audio_segment = self._normalize_volume(audio_segment)

            if self.has_scipy:
                audio_segment = self._echo_cancellation(audio_segment)

            audio_segment.export(output_path, format="wav")

            return {"success": True, "message": "音频增强完成", "output_path": output_path}

        except Exception as e:
            return {"success": False, "message": f"音频增强失败: {str(e)}"}
```
**解释**：
- 处理流程：降噪 → 音量标准化 → 回声消除
- 每个步骤都检查依赖库是否可用

```python
    def _denoise(self, audio_segment):
        try:
            import noisereduce as nr
            samples = np.array(audio_segment.get_array_of_samples(), dtype=np.float32)
            if audio_segment.channels == 2:
                samples = samples.reshape((-1, 2)).mean(axis=1)
            reduced_samples = nr.reduce_noise(y=samples, sr=audio_segment.frame_rate)
            reduced_samples = (reduced_samples * 32767).astype(np.int16)
            return audio_segment._spawn(reduced_samples.tobytes())
        except Exception as e:
            return audio_segment
```
**解释**：
- `noisereduce`：基于频谱门控的降噪算法
- 将 pydub 音频段转为 numpy 数组
- 立体声转单声道
- 降噪后转回 int16 格式

```python
    def _normalize_volume(self, audio_segment, target_dbfs=-20):
        try:
            change_in_dbfs = target_dbfs - audio_segment.dBFS
            return audio_segment.apply_gain(change_in_dbfs)
        except Exception as e:
            return audio_segment
```
**解释**：
- 计算当前音量与目标音量的差值
- 应用增益调整
- `target_dbfs=-20`：目标平均音量为 -20dBFS

```python
    def _echo_cancellation(self, audio_segment):
        try:
            import scipy.signal
            samples = np.array(audio_segment.get_array_of_samples(), dtype=np.float64)
            if audio_segment.channels == 2:
                samples = samples.reshape((-1, 2)).mean(axis=1)

            sr = audio_segment.frame_rate
            delay_ms = 50
            delay_samples = int(sr * delay_ms / 1000)
            filter_length = 128
            mu = 0.01

            ref = np.zeros(len(samples), dtype=np.float64)
            if delay_samples < len(samples):
                ref[delay_samples:] = samples[:-delay_samples]

            weights = np.zeros(filter_length, dtype=np.float64)
            output = np.zeros(len(samples), dtype=np.float64)

            for i in range(filter_length, len(samples)):
                x = ref[i - filter_length:i][::-1].copy()
                y = np.dot(weights, x)
                e = samples[i] - y
                weights += mu * e * x
                output[i] = e

            max_val = np.max(np.abs(output)) if np.max(np.abs(output)) > 0 else 1.0
            output = (output / max_val * 32767).astype(np.int16)

            return audio_segment._spawn(output.tobytes())
        except Exception as e:
            return audio_segment
```
**解释**：
- LMS (Least Mean Squares) 自适应滤波器
- `delay_ms = 50`：预估回声延迟
- `filter_length = 128`：滤波器长度
- `mu = 0.01`：步长参数（学习率）
- `ref`：参考信号（延迟版本）
- `weights`：滤波器权重
- `y = np.dot(weights, x)`：滤波器输出（回声估计）
- `e = samples[i] - y`：误差信号（消除回声后）
- `weights += mu * e * x`：更新权重

---

# 九、speaker_diarization.py — 说话人识别模块完整代码讲解

```python
class SpeakerDiarization:
    def diarize(self, audio_path: str) -> list:
        if not os.path.exists(audio_path):
            return [{"speaker": "未知发言人", "start": 0.0, "end": 0.0}]

        if not self.has_resemblyzer or not self.has_sklearn:
            return [{"speaker": "未知发言人", "start": 0.0, "end": 0.0}]

        try:
            audio, duration = self._load_audio(audio_path)
            if audio is None:
                return [{"speaker": "未知发言人", "start": 0.0, "end": duration}]

            segments = self._vad_segment(audio, duration)
            if not segments:
                return [{"speaker": "未知发言人", "start": 0.0, "end": duration}]

            embeddings = self._extract_embeddings(audio, segments)
            if len(embeddings) == 0:
                return [{"speaker": "未知发言人", "start": 0.0, "end": duration}]

            labels = self._cluster_speakers(embeddings)

            result = []
            for i, (start, end) in enumerate(segments):
                if i < len(labels):
                    speaker = f"Speaker_{labels[i] + 1}"
                else:
                    speaker = "Speaker_1"
                result.append({
                    "speaker": speaker,
                    "start": round(start, 2),
                    "end": round(end, 2),
                })

            return result
        except Exception as e:
            return [{"speaker": "未知发言人", "start": 0.0, "end": 0.0}]
```
**解释**：
- 完整流程：加载音频 → VAD → 声纹提取 → 聚类 → 输出结果

```python
    def _vad_segment(self, audio, duration: float) -> list:
        sample_rate = 16000
        frame_length = int(sample_rate * 0.025)
        hop_length = int(sample_rate * 0.010)
        
        segments = []
        in_speech = False
        start_time = 0.0
        
        for i in range(0, len(audio) - frame_length, hop_length):
            frame = audio[i:i + frame_length]
            rms = np.sqrt(np.mean(frame ** 2))
            
            if rms > ENERGY_THRESHOLD:
                if not in_speech:
                    start_time = i / sample_rate
                    in_speech = True
            else:
                if in_speech:
                    end_time = i / sample_rate
                    if end_time - start_time >= MIN_SEGMENT_DURATION:
                        segments.append((start_time, end_time))
                    in_speech = False
        
        if in_speech:
            end_time = len(audio) / sample_rate
            if end_time - start_time >= MIN_SEGMENT_DURATION:
                segments.append((start_time, end_time))
        
        return segments
```
**解释**：
- VAD (Voice Activity Detection)：语音活动检测
- `frame_length = 25ms`：帧长
- `hop_length = 10ms`：帧移
- `rms = np.sqrt(np.mean(frame ** 2))`：计算 RMS 能量
- `ENERGY_THRESHOLD`：能量阈值，超过则认为是语音
- `MIN_SEGMENT_DURATION`：最短语音段时长

```python
    def _extract_embeddings(self, audio, segments: list) -> list:
        from resemblyzer import VoiceEncoder
        encoder = VoiceEncoder()
        
        embeddings = []
        
        for start, end in segments:
            start_sample = int(start * SAMPLE_RATE)
            end_sample = int(end * SAMPLE_RATE)
            segment = audio[start_sample:end_sample]
            
            target_length = int(1.6 * SAMPLE_RATE)
            
            if len(segment) < target_length:
                segment = np.pad(segment, (0, target_length - len(segment)))
            elif len(segment) > target_length:
                start_idx = (len(segment) - target_length) // 2
                segment = segment[start_idx:start_idx + target_length]
            
            embedding = encoder.embed_utterance(segment)
            embeddings.append(embedding)
        
        return embeddings
```
**解释**：
- `VoiceEncoder`：基于 GE2E 损失训练的 LSTM 声纹编码器
- 输入：1.6 秒音频片段（16kHz 采样率）
- 输出：256 维向量
- 如果音频太短，填充静音；如果太长，截取中间部分

```python
    def _cluster_speakers(self, embeddings: list) -> list:
        from sklearn.cluster import AgglomerativeClustering
        from sklearn.metrics.pairwise import cosine_similarity

        if len(embeddings) < 2:
            return [0] * len(embeddings)

        similarity_matrix = cosine_similarity(embeddings)
        distance_matrix = 1 - similarity_matrix

        n_speakers = self._estimate_speaker_count(distance_matrix)

        clustering = AgglomerativeClustering(
            n_clusters=n_speakers,
            metric='precomputed',
            linkage='average'
        )

        labels = clustering.fit_predict(distance_matrix)
        return labels
```
**解释**：
- `cosine_similarity`：计算余弦相似度矩阵
- `distance_matrix = 1 - similarity_matrix`：转为距离矩阵
- `AgglomerativeClustering`：层次聚类
- `linkage='average'`：平均链接

---

# 十、streaming_service.py — 实时转写模块完整代码讲解

```python
class StreamingTranscriber:
    def __init__(self, sample_rate=16000, chunk_duration=3.0, overlap_duration=0.5):
        self.sample_rate = sample_rate
        self.chunk_duration = chunk_duration
        self.overlap_duration = overlap_duration
        self.buffer = bytearray()
        self._context_text = ""
        self._input_sample_rate = None
        self._model = None
```
**解释**：
- `sample_rate=16000`：Whisper 要求 16kHz 采样率
- `chunk_duration=3.0`：每次识别 3 秒音频
- `buffer`：音频缓冲区
- `_context_text`：上下文文本，提升识别连贯性

```python
    def add_audio(self, audio_data: bytes):
        self.buffer.extend(audio_data)

    def should_transcribe(self) -> bool:
        input_rate = self._input_sample_rate or self.sample_rate
        expected_bytes = int(input_rate * 2 * (self.chunk_duration + self.overlap_duration))
        return len(self.buffer) >= expected_bytes
```
**解释**：
- `add_audio`：将浏览器发送的音频数据添加到缓冲区
- `should_transcribe`：检查缓冲区是否达到识别阈值
- `input_rate * 2`：16-bit PCM，每采样 2 字节

```python
    def transcribe_chunk(self) -> dict:
        self._init_model()
        
        if self._model is None:
            return {"text": "", "is_final": False}

        samples = np.frombuffer(self.buffer, dtype=np.int16)
        audio_np = samples.astype(np.float32) / 32768.0

        audio_np = self._resample_to_16k(audio_np)

        if self._is_silence(audio_np):
            self.buffer = bytearray()
            return {"text": "", "is_final": False}

        segments, _ = self._model.transcribe(
            audio_np,
            language="zh",
            beam_size=5,
            initial_prompt=self._context_text
        )

        text = "".join([seg.text for seg in segments])

        if self._is_hallucination(text):
            text = ""

        self._context_text = (self._context_text + text)[-200:]

        self.buffer = bytearray()

        return {"text": text, "is_final": False}
```
**解释**：
- 将缓冲区转为 numpy 数组
- 重采样到 16kHz
- 静音检测
- 使用 Whisper 识别
- 幻觉检测
- 更新上下文（保留最近 200 字符）
- 清空缓冲区

---

# 十一、routes/*.py — API路由层完整代码讲解

```python
# routes/meetings.py
router = APIRouter(prefix="/api/meetings", tags=["会议管理"])

@router.post("/upload-audio")
async def upload_audio(file: UploadFile = File(...), user: User = Depends(require_user)):
    logger.info("/upload-audio called, filename=%s", file.filename)
    result = await meeting_service.save_audio(file)
    if not result["success"]:
        return APIResponse(success=False, message=result["message"])
    return APIResponse(success=True, message=result["message"], data=result["data"])
```
**解释**：
- `APIRouter`：FastAPI 路由器
- `prefix="/api/meetings"`：路由前缀
- `@router.post("/upload-audio")`：POST 请求的 `/api/meetings/upload-audio` 端点
- `file: UploadFile = File(...)`：文件上传参数
- `user: User = Depends(require_user)`：依赖注入，自动认证用户

```python
@router.post("")
async def create_meeting(data: MeetingCreate, db: Session = Depends(get_db), user: User = Depends(require_user)):
    result = meeting_service.create_meeting(data, db)
    if result.get("success") and result.get("data", {}).get("id"):
        meeting = db.query(Meeting).filter(Meeting.id == result["data"]["id"]).first()
        if meeting:
            meeting.created_by = user.id
            db.commit()
    return APIResponse(**result)
```
**解释**：
- `data: MeetingCreate`：请求体，自动解析 JSON
- `db: Session = Depends(get_db)`：数据库会话
- `user: User = Depends(require_user)`：当前用户

```python
@router.get("")
async def list_meetings(page: int = 1, size: int = 20, db: Session = Depends(get_db), user: User = Depends(require_user)):
    from sqlalchemy import func
    subq = (
        db.query(
            Meeting,
            func.count().over().label("total_count"),
        )
        .filter(Meeting.created_by == user.id)
        .order_by(Meeting.created_at.desc())
        .offset((page - 1) * size)
        .limit(size)
        .all()
    )

    total = subq[0][1] if subq else 0
    meetings = [row[0] for row in subq]

    return APIResponse(success=True, data={
        "total": total, "page": page, "size": size,
        "items": [to_response(MeetingResponse, m) for m in meetings],
    })
```
**解释**：
- 分页查询
- `func.count().over()`：窗口函数，计算总数
- `offset((page - 1) * size)`：偏移量
- `limit(size)`：每页数量

---

# 十二、app.js — 前端交互逻辑完整代码讲解

```javascript
const API_BASE = '';

class MeetingApp {
    constructor() {
        this.currentMeetingId = null;
        this.currentMinutes = null;
        this.actionItems = [];
        this.currentUser = null;
        this.sessionToken = null;
        this.currentTemplate = null;
        this.templates = [];
        this.isRecording = false;
        this._progressTimers = [];
        this._mediaRecorder = null;
        this._audioChunks = [];
        this._recordStream = null;
        this._ws = null;
    }

    async init() {
        this._bindEvents();
        this._bindKeyboardShortcuts();
        this._loadDraft();
        this._setupAutoSave();
        this._setupInputValidation();
        this._cacheDOMElements();
        await this._checkLoginStatus();
        await this.loadHistory();
    }
}
```
**解释**：
- 状态管理：当前会议、纪要、行动项、用户信息
- 录音相关：MediaRecorder、音频块、流
- WebSocket：实时转写连接
- `init()`：初始化方法，绑定事件、加载草稿、检查登录状态

```javascript
    _bindEvents() {
        const dropZone = document.getElementById('dropZone');
        const fileInput = document.getElementById('fileInput');

        dropZone.addEventListener('click', () => fileInput.click());
        dropZone.addEventListener('dragover', (e) => {
            e.preventDefault();
            dropZone.classList.add('drag-over');
        });
        dropZone.addEventListener('dragleave', () => dropZone.classList.remove('drag-over'));
        dropZone.addEventListener('drop', (e) => {
            e.preventDefault();
            dropZone.classList.remove('drag-over');
            if (e.dataTransfer.files.length) this.handleFile(e.dataTransfer.files[0]);
        });
        fileInput.addEventListener('change', (e) => {
            if (e.target.files.length) this.handleFile(e.target.files[0]);
        });

        document.getElementById('btnSubmitText').addEventListener('click', () => this.submitText());
        document.getElementById('btnSubmitRecord').addEventListener('click', () => this.submitRecordResult());
        document.getElementById('btnGenerateMinutes').addEventListener('click', () => this.generateMinutes());
        document.getElementById('btnExportMD').addEventListener('click', () => this.exportMarkdown());
        document.getElementById('btnExportDocx').addEventListener('click', () => this.exportDocx());
        document.getElementById('btnAddAction').addEventListener('click', () => this.addActionItem());
    }
```
**解释**：
- 拖拽上传：dragover、dragleave、drop 事件
- 点击上传：click 事件
- 按钮点击：submitText、generateMinutes、exportMarkdown 等

```javascript
    async handleFile(file) {
        const ext = file.name.split('.').pop().toLowerCase();
        const allowed = ['mp3', 'wav', 'm4a', 'aac', 'wma', 'ogg', 'flac'];
        if (!allowed.includes(ext)) {
            this.showToast('不支持的音频格式：.' + ext, 'error');
            return;
        }

        const fileSize = (file.size / 1024 / 1024).toFixed(1);
        this.showProgress('上传中…', 0, file.name + ' (' + fileSize + ' MB)');

        try {
            const formData = new FormData();
            formData.append('file', file);

            const uploadResult = await this._uploadWithProgress(formData, fileSize);
            const filename = uploadResult.data.filename;

            this.updateProgress('语音识别中…', 10, '音频已上传，开始识别…');
            this.currentAudioFile = filename;
            this._startProgressAnimation();

            const asrEngine = document.getElementById('asrSelect').value;
            const resp2 = await fetch(API_BASE + '/api/meetings/recognize', {
                method: 'POST',
                headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
                body: 'filepath=' + encodeURIComponent(filename) + '&asr_engine=' + encodeURIComponent(asrEngine),
            });
            const data2 = await resp2.json();
            this._clearProgressTimers();

            if (!data2.success) throw new Error(data2.message);

            this.completeProgress('语音识别完成!');
            document.getElementById('recordResultText').textContent = data2.data.text;
            document.getElementById('tabBtnResult').style.display = '';
            this.switchTab('result');

            if (!document.getElementById('meetingTitle').value) {
                const nameWithoutExt = file.name.replace(/\.[^.]+$/, '');
                document.getElementById('meetingTitle').value = nameWithoutExt + ' - Meeting';
            }
        } catch (err) {
            this._clearProgressTimers();
            this.errorProgress(err.message);
            this.showToast(err.message, 'error');
        }
    }
```
**解释**：
- 验证文件格式
- 上传文件到服务器
- 调用语音识别 API
- 显示进度
- 成功后显示结果

```javascript
    async submitText() {
        if (!this.requireLogin('提交')) return;

        const text = document.getElementById('textInput').value.trim();
        if (!text) {
            this.showToast('请输入会议内容', 'warning');
            return;
        }

        this.showProgress('分析中…', 10, '正在分析会议内容…');

        try {
            const resp1 = await fetch(API_BASE + '/api/meetings', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    title: document.getElementById('meetingTitle').value || undefined,
                    meeting_date: document.getElementById('meetingDate').value || undefined,
                    participants: document.getElementById('participants').value || undefined,
                    input_type: 'text',
                    raw_text: text,
                    is_public: document.getElementById('isPublic').checked,
                }),
            });
            const data1 = await resp1.json();
            if (!data1.success) throw new Error(data1.message);

            this.currentMeetingId = data1.data.id;
            this.updateProgress('生成纪要…', 50, '正在提取关键信息…');

            const resp2 = await fetch(API_BASE + `/api/meetings/${this.currentMeetingId}/generate-minutes`, {
                method: 'POST',
            });
            const data2 = await resp2.json();
            if (!data2.success) throw new Error(data2.message);

            this.completeProgress('分析完成!');
            this.currentMinutes = data2.data;
            this.actionItems = data2.data.action_items || [];

            this._clearDraft();
            this._showMinutesResult(data2.data);
            await this.loadHistory();

        } catch (err) {
            this.errorProgress(err.message);
            this.showToast(err.message, 'error');
        }
    }
```
**解释**：
- 创建会议记录
- 生成会议纪要
- 显示结果

```javascript
    showProgress(label, percent, detail) {
        const el = this._els;
        el.progressSection.style.display = 'block';
        el.progressSection.classList.remove('completed', 'error');
        el.progressLabel.textContent = label;
        el.progressPercent.textContent = percent + '%';
        el.progressBar.style.width = percent + '%';
        el.progressDetail.textContent = detail;
    }

    updateProgress(label, percent, detail) {
        const el = this._els;
        el.progressLabel.textContent = label;
        el.progressPercent.textContent = percent + '%';
        el.progressBar.style.width = percent + '%';
        if (detail) el.progressDetail.textContent = detail;
    }

    completeProgress(detail) {
        const el = this._els;
        el.progressSection.classList.add('completed');
        el.progressBar.classList.remove('animating');
        this.updateProgress('完成', '100', detail);
    }

    errorProgress(detail) {
        const el = this._els;
        el.progressSection.classList.add('error');
        el.progressBar.classList.remove('animating');
        this.updateProgress('失败', '0', detail);
    }
```
**解释**：
- 进度条管理方法
- `showProgress`：显示进度条
- `updateProgress`：更新进度
- `completeProgress`：完成
- `errorProgress`：失败

---

**答辩人**：[姓名]  
**日期**：2026年7月
